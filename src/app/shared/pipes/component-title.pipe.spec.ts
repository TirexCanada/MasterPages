import { ComponentTitlePipe } from './component-title.pipe';

describe('ComponentTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
